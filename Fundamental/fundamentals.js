// Variables--------------------------------------------
var item1 = "hello"; //String 
var item4 = 20; // Numbers
var item7 = true; // Boolean
var item11 = a; // Assignment  

let item2 = "programing"; //String 
let item5 = 25; // Numbers
let item8 = false; // Boolean
let item12= b; // Assignment 

const item3Const = "world"; //String 
const item6 = 30; // Numbers
const item10 = true; // Boolean
const item13 = c; // Assignment  




//Store multiple items---------------------------------
// List
var array = ["item1", 3, true, item1]; 
let array = ["luis", 30, false];
const arrayConst = ["item1", 3, true, item1];


// Objects
const familyObj ={
    firstName: "Luis", 
    lastName: "Cardona",
    title: "father",
};

console

// Loops:----------------------------------------------

for(var i = 0; i < ar.length; i++){
    // code block to be executed
    return
};

for (key in object) {
    // code block to be executed
  };
  

for (variable of iterable) {
    // code block to be executed
};


while (1 < 10) {
    // code block to be executed
    i++;
  };




// Conditional Statements:-----------------------------------------------
// if statement
if ("test" === "test") {
    console.log("true");
    
}

// if else statement
if ("test" === "test1") {
    console.log("true");
    
} else {
    console.log("false");
    
}


switch(expression) {
    case x:
      // code block
      break;
    case y:
      // code block
      break;
    default:
      // code block
  }

  switch (new Date().getDay()) {
    case 0:
      day = "Sunday";
      break;
    case 1:
      day = "Monday";
      break;
    case 2:
       day = "Tuesday";
      break;
    case 3:
      day = "Wednesday";
      break;
    case 4:
      day = "Thursday";
      break;
    case 5:
      day = "Friday";
      break;
    case 6:
      day = "Saturday";
  }

// Functions: use to execute a set of instruction when called.

function simpleFunction(e, p1, p2) {
    // code to be executed
};

const arrowFunction = (e, p1, p2) => {
    // code to be executed
};


// Using them all together:
